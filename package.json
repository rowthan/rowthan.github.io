{
  "name": "rowthan-tools",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Open-source browser tools by Rowthan",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "typescript": "~5.8.3",
    "vite": "^7.0.5",
    "vitest": "^3.2.4"
  },
  "license": "MIT"
}
